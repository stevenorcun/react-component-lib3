// TODO localization?
export enum CasePriorityTranslation {
  Low = 'Faible',
  Medium = 'Moyen',
  High = 'Élevé',
}

// TODO localization?
export enum CaseStatusTranslation {
  Initiated = 'Initié',
  Active = 'En cours',
  Ended = 'Terminée',
}

// TODO localization?
export enum CaseClassificationTranslation {
  Public = 'Publique',
  Restricted = 'Restreint',
  Secret = 'Secret',
}
